{{#*inline "tablePartial"}}

export interface {{interfaceName}} {{#if extends}}extends {{extends}} {{/if}}{
  {{#each additionalProperties as |prop|}}
  {{prop}}
  {{/each}}
  {{#each columns}}
  "{{propertyName}}": {{propertyType}} {{#if nullable}}| null {{/if}}
  {{/each}}
}
{{/inline}}
/*
* This file was auto generated generated by @rmp135/sql-ts.
* Rerun yarn gen-table-types to regenerate this file.
*
* Columns with default values are still represented as required fields
* in the interfaces to represent the output of each entity.
*/
{{#if config.schemaAsNamespace}}
{{#each grouped as |tables key|}}
export namespace {{key}} {
{{#each tables as |table|}}
  {{> tablePartial table}}
{{/each}}
{{/each}}
}
{{else}}
{{#each tables as |table|}}
{{> tablePartial table}}
{{/each}}
{{/if}}
